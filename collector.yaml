receivers:
  otlp:
    protocols:
      grpc:
      http:

exporters:
  logging:
    loglevel: info
  otlp:
    endpoint: "api.honeycomb.io:443"
    headers:
      "x-honeycomb-team": "zewOz9u27ik8uAW12ER8PA"
      "x-honeycomb-dataset": "test-lambda"

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [logging, otlp]